### Beer Temp Server

---

Dependencies:

mosquitto

```Bash
git clone https://github.com/eclipse/mosquitto
mkdir build
cd build
cmake ..
make 
sudo make install
## you may additionally need to update ldconfig manually
sudo ldconfig
```

